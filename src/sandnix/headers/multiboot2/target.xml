<?xml version="1.0" encoding="utf-8"?><target name="multiboot2" type="build">
    <output name="kernel.multiboot2"/>
    <outdir path="../../../../bin/$(ARCH)/sandnix"/>
    <middir path="../../../../obj/$(ARCH)/sandnix"/>
    <introduction>
        Multiboot2 header.X86 only.
    </introduction>
    <archs>
        <arch name="x86">
            <PREV></PREV>
            <DEP>gcc</DEP>
            <DEPRULE>$(DEP) -MM -o $@ $&lt;;if [ $$? == 0 ];then cat $@|awk 'NR == 1 {printf(&quot;%s%s\n&quot;,&quot;$(OBJDIR)/$(ARCH)/&quot;,$$0)} NR != 1 {print $$0}' &gt; $@; fi</DEPRULE>
            <CC>gcc</CC>
            <CFLAGS>-c</CFLAGS>
            <CCRULE>$(CC) $(CFLAGS) -o $@ $&lt;</CCRULE>
            <AS>gcc</AS>
            <ASFLAGS>-x assembler-with-cpp -c -m32 -D_ASM</ASFLAGS>
            <ASRULE>$(AS) $(ASFLAGS) -o $@ $&lt;</ASRULE>
            <LD>ld</LD>
            <LDFLAGS>--oformat binary -m elf_i386 -N -Ttext 0x0000</LDFLAGS>
            <LDRULE>$(LD) $(LDFLAGS) -o $@ $^</LDRULE>
            <AFTER>./image-builder.py -h $(LINKED) -i $(OUTDIR)/kernel -o $(OUTDIR)/$(OUTPUT)</AFTER>
        </arch>
    </archs>
    <dependencies>
    </dependencies>
    <sub-targets>
    </sub-targets>
    <options>
    </options>
</target>